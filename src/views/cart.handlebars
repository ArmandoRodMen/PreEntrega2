<h2>Los productos del carrito {{idCart}} son:</h2>

{{#each products}}
<div>

    <!--
    <h3>{{this.product.title}}</h3>
    <p>Descripción: {{this.product.description}}</p>
    <p>Precio: {{this.product.price}}</p>
    <p>Stock: {{this.product.stock}}</p>
    <p>Categoría: {{this.product.category}}</p>
    <img src="{{this.product.thumbnail}}" width="100" alt="{{this.product.description}}">-->
    <p>Cantidad: {{quantity}}</p>
</div>
{{/each}}
